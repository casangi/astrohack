from astropy.coordinates import EarthLocation
import numpy as np


def _locit_chunk(locit_parms):
    print(f'procesing antenna {locit_parms["this_ant"]} ddi {locit_parms["this_ddi"]}')
    # for time in times:
    #     compute lst at antenna pos
    #     compute elevation
    #     if above eleveation_limit:
    #         add to system
    # solve_system
    # store data in container
    return
